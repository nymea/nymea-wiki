# WiFi Detector Plugin
--------------------------------------------

This plugin allows to find and monitor network devices in your local network by using the MAC address.

> **Note**: the application `nmap` has to be installed.

